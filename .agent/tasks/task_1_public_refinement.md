# Task: Refine Public Structure and Navigation

Refine the core application structure and the Public Homepage to ensure a seamless "Tech-Noir" experience. This includes fixing the navigation flow, ensuring the theme toggle works system-wide, and aligning the homepage content with the `webrebuild.html` reference.

## 🎯 Deliverables
1. **PublicHomepage.tsx**: Complete content parity with `webrebuild.html`.
2. **Context Integration**: Ensure `useTheme` and `useNavigation` are fully leveraged.
3. **Navigation Bar**: Responsive glass navbar with theme toggle and hexagonal logo.

## 🛠️ Step-by-Step
1. [x] Update `PublicHomepage.tsx` with the exact text and descriptions from the reference file.
2. [x] Refine the `TechCard` and `StageCard` sub-components for better visual fidelity.
3. [x] Implement the smooth scroll navigation using `id` anchors.
4. [x] Verify "Dark Mode" vs "Light Mode" contrast on all text elements.
